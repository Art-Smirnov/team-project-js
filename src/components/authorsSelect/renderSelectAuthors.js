// import getRefs from '../../services/get-refs.js';
// const refs = getRefs();
// // console.log(refs);

// export default function renderSelectAuthors(arr) {
//   const option = arr.map(({ name }) => {
//     return `<option value="${name}">${name}</option>`;
//   });
//   refs.selectAuthor.insertAdjacentHTML('beforeend', option);
// }

// refs.selectAuthor.addEventListener('click', () => {
//   if (refs.selectAuthor.classList.contains('is-active')) {
//     refs.selectAuthor.classList.remove('is-active');
//     return;
//   }
//   refs.selectAuthor.classList.add('is-active');
// });

// refs.selectAuthor.addEventListener('blur', () => {
//   refs.selectAuthor.classList.remove('is-active');
// });
